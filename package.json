{
  "name": "quorum",
  "version": "0.1.0",
  "description": "Provider-agnostic multi-model consensus review for AI-assisted code changes",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "quorum": "src/cli/index.ts"
  },
  "scripts": {
    "quorum": "bun run src/cli/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
